<?xml version="1.0" encoding="UTF-8"?>
<!--
  logback-spring.xml — Booking Service Structured Logging Configuration
  Outputs JSON-structured logs in production for log aggregation (ELK, Loki, etc.)
  Uses pattern format in dev for human readability.
-->
<configuration>

    <!-- ============================================================
         SPRING PROFILE: dev — Human-readable pattern with color
         ============================================================ -->
    <springProfile name="dev">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>
                    %d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) [%thread] %cyan([traceId=%X{traceId:-NONE} spanId=%X{spanId:-NONE} correlationId=%X{correlationId:-NONE}]) %logger{36} - %msg%n
                </pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>

        <logger name="com.labplatform.booking" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>
        <logger name="org.springframework.security" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>
        <logger name="org.hibernate.SQL" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>
        <logger name="io.github.resilience4j" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>
    </springProfile>

    <!-- ============================================================
         SPRING PROFILE: prod — JSON structured logs for log aggregation
         (ELK Stack, Grafana Loki, Splunk, etc.)
         Each log line is a single JSON object parseable by log shippers.
         ============================================================ -->
    <springProfile name="prod">
        <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <!-- Standard fields -->
                <fieldNames>
                    <timestamp>@timestamp</timestamp>
                    <message>message</message>
                    <logger>logger</logger>
                    <thread>thread</thread>
                    <levelValue>[ignore]</levelValue>
                </fieldNames>
                <!-- Include MDC fields in every log line -->
                <includeMdcKeyName>traceId</includeMdcKeyName>
                <includeMdcKeyName>spanId</includeMdcKeyName>
                <includeMdcKeyName>correlationId</includeMdcKeyName>
                <includeMdcKeyName>eventId</includeMdcKeyName>
                <!-- Static fields added to every log line -->
                <customFields>{"service":"booking-service","environment":"prod"}</customFields>
            </encoder>
        </appender>

        <root level="WARN">
            <appender-ref ref="JSON_CONSOLE"/>
        </root>

        <logger name="com.labplatform.booking" level="INFO" additivity="false">
            <appender-ref ref="JSON_CONSOLE"/>
        </logger>
    </springProfile>

</configuration>
